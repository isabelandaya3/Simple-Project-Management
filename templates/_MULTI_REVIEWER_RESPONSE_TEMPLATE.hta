<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta charset="windows-1252">
    <title>Initial Review Response - {{ITEM_IDENTIFIER}}</title>
    <HTA:APPLICATION
        ID="MultiReviewerResponseForm"
        APPLICATIONNAME="Multi-Reviewer Response"
        BORDER="dialog"
        BORDERSTYLE="normal"
        CAPTION="yes"
        CONTEXTMENU="yes"
        MAXIMIZEBUTTON="yes"
        MINIMIZEBUTTON="yes"
        SCROLL="yes"
        SCROLLFLAT="yes"
        SELECTION="yes"
        SHOWINTASKBAR="yes"
        SINGLEINSTANCE="no"
        SYSMENU="yes"
        VERSION="1.0"
        WINDOWSTATE="normal"
    />
    <style>
        * { box-sizing: border-box; margin: 0; padding: 0; }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }
        .container {
            background: white;
            border-radius: 16px;
            box-shadow: 0 25px 50px rgba(0,0,0,0.15);
            padding: 40px;
            max-width: 700px;
            width: 100%;
        }
        h1 {
            color: #1a1a2e;
            font-size: 24px;
            margin-bottom: 8px;
        }
        .subtitle {
            color: #666;
            font-size: 14px;
            margin-bottom: 8px;
        }
        .reviewer-badge {
            display: inline-block;
            background: #10b981;
            color: white;
            padding: 4px 12px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            margin-bottom: 24px;
        }
        .info-box {
            background: #f8f9fa;
            border-radius: 8px;
            padding: 16px;
            margin-bottom: 24px;
        }
        .info-row {
            display: flex;
            margin-bottom: 8px;
        }
        .info-row:last-child { margin-bottom: 0; }
        .info-label {
            font-weight: 600;
            color: #444;
            width: 160px;
            flex-shrink: 0;
        }
        .info-value { color: #666; }
        .section-title {
            font-size: 16px;
            font-weight: 600;
            color: #1a1a2e;
            margin-bottom: 12px;
        }
        .form-group { margin-bottom: 20px; }
        label {
            display: block;
            font-weight: 500;
            color: #444;
            margin-bottom: 6px;
        }
        select, textarea {
            width: 100%;
            padding: 10px 12px;
            border: 1px solid #ddd;
            border-radius: 8px;
            font-size: 14px;
            font-family: inherit;
        }
        select:focus, textarea:focus {
            outline: none;
            border-color: #667eea;
            box-shadow: 0 0 0 3px rgba(102,126,234,0.1);
        }
        textarea { resize: vertical; min-height: 100px; }
        
        .bluebeam-notice {
            background: #dbeafe;
            border: 1px solid #3b82f6;
            border-radius: 8px;
            padding: 16px;
            margin-bottom: 24px;
        }
        .bluebeam-notice h3 {
            margin: 0 0 8px 0;
            color: #1e40af;
            font-size: 14px;
        }
        .bluebeam-notice p {
            color: #1e40af;
            margin: 0;
            font-size: 13px;
        }
        
        .btn {
            display: inline-block;
            padding: 14px 28px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 8px;
            font-size: 16px;
            font-weight: 600;
            cursor: pointer;
            width: 100%;
        }
        .btn:hover {
            opacity: 0.9;
        }
        .btn:disabled {
            background: #ccc;
            cursor: not-allowed;
        }
        
        .success-box {
            background: #d4edda;
            border: 1px solid #28a745;
            border-radius: 8px;
            padding: 20px;
            margin-bottom: 20px;
            color: #155724;
            text-align: center;
        }
        .hidden { display: none !important; }
        
        .open-folder-btn {
            display: inline-block;
            margin-top: 10px;
            padding: 8px 16px;
            background: #17a2b8;
            color: white;
            border: none;
            border-radius: 6px;
            font-size: 13px;
            cursor: pointer;
        }
        .open-folder-btn:hover { background: #138496; }
    </style>
</head>
<body>
    <div class="container">
        <h1>Initial Review Response</h1>
        <p class="subtitle">{{ITEM_TYPE}} {{ITEM_IDENTIFIER}}</p>
        <p><span class="reviewer-badge">Reviewer: {{REVIEWER_NAME}}</span></p>
        
        <div class="info-box">
            <div class="info-row">
                <span class="info-label">Title:</span>
                <span class="info-value">{{ITEM_TITLE}}</span>
            </div>
            <div class="info-row">
                <span class="info-label">Date Received:</span>
                <span class="info-value">{{DATE_RECEIVED}}</span>
            </div>
            <div class="info-row">
                <span class="info-label">Your Due Date:</span>
                <span class="info-value" style="color: #2980b9; font-weight: bold;">{{REVIEWER_DUE_DATE}}</span>
            </div>
        </div>

        <!-- Bluebeam Notice instead of file selection -->
        <div class="bluebeam-notice">
            <h3>Markups Instructions</h3>
            <p><strong>Provide markups in the corresponding Bluebeam session.</strong></p>
            <p style="margin-top: 8px;">Do not attach files here. All markups should be completed in the shared Bluebeam Studio session for this item.</p>
        </div>

        <!-- RFI Question Section (shown only for RFI items) -->
        <div id="rfi-question-section" class="info-box" style="background: #fff8e1; border: 2px solid #ffc107; display: none;">
            <div style="font-size: 15px; color: #f57c00; font-weight: bold; margin-bottom: 10px;">&#10067; RFI Question</div>
            <div id="rfi-question-text" style="font-size: 14px; color: #333; line-height: 1.6; white-space: pre-wrap;">{{RFI_QUESTION}}</div>
        </div>

        <div id="success-message" class="success-box hidden"></div>

        <form id="response-form">
            <div class="form-group">
                <label for="response_category">Response Category *</label>
                <select name="response_category" id="response_category">
                    <option value="">-- Select Response Category --</option>
                    <option value="Approved">Approved</option>
                    <option value="Approved as Noted">Approved as Noted</option>
                    <option value="For Record Only">For Record Only</option>
                    <option value="Rejected">Rejected</option>
                    <option value="Revise and Resubmit">Revise and Resubmit</option>
                </select>
            </div>
            
            <div class="form-group">
                <label for="internal_notes">Suggested response for QC Reviewer <span style="font-weight: normal; color: #888;">(team only - not shared externally)</span></label>
                <textarea name="internal_notes" id="internal_notes" placeholder="Provide your suggested response text or notes for the QC Reviewer to consider..."></textarea>
            </div>
            
            <button type="button" class="btn" id="submit-btn" onclick="submitResponse()">Submit Review</button>
        </form>
    </div>

    <script language="JavaScript">
        var ITEM_CONFIG = {
            item_id: {{ITEM_ID}},
            item_type: "{{ITEM_TYPE}}",
            identifier: "{{ITEM_IDENTIFIER}}",
            title: "{{ITEM_TITLE}}",
            token: "{{TOKEN}}",
            reviewer_name: "{{REVIEWER_NAME}}",
            reviewer_email: "{{REVIEWER_EMAIL}}",
            folder_path: "{{FOLDER_PATH}}",
            is_rfi: {{IS_RFI}}
        };
        
        var fso = new ActiveXObject("Scripting.FileSystemObject");
        var shell = new ActiveXObject("WScript.Shell");

        // Initialize on load
        window.onload = function() {
            // Show RFI question section if this is an RFI
            if (ITEM_CONFIG.is_rfi) {
                var rfiSection = document.getElementById("rfi-question-section");
                if (rfiSection) {
                    rfiSection.style.display = "block";
                }
            }
        };

        function submitResponse() {
            var category = document.getElementById('response_category').value;
            var internalNotes = document.getElementById('internal_notes').value;
            
            if (!category) {
                alert('Please select a response category.');
                return;
            }
            
            // Build response object - multi-reviewer format
            var response = {
                _form_type: "multi_reviewer_response",
                _version: "1.0",
                _submitted_at: new Date().toISOString(),
                item_id: ITEM_CONFIG.item_id,
                identifier: ITEM_CONFIG.identifier,
                token: ITEM_CONFIG.token,
                reviewer_name: ITEM_CONFIG.reviewer_name,
                reviewer_email: ITEM_CONFIG.reviewer_email,
                response_category: category,
                internal_notes: internalNotes
            };
            
            var jsonString = JSON.stringify(response, null, 2);
            
            // Save to Responses folder
            var responsesFolder = ITEM_CONFIG.folder_path + "\\Responses";
            
            // Use reviewer name in filename to avoid conflicts
            var safeName = ITEM_CONFIG.reviewer_name.replace(/[^a-zA-Z0-9]/g, "_");
            var filePath = responsesFolder + "\\_multi_reviewer_response_" + safeName + ".json";
            
            try {
                // Ensure Responses folder exists
                if (!fso.FolderExists(responsesFolder)) {
                    fso.CreateFolder(responsesFolder);
                }
                
                var file = fso.CreateTextFile(filePath, true, false);
                file.Write(jsonString);
                file.Close();
                
                // Success!
                document.getElementById('response-form').className = 'hidden';
                document.getElementById('success-message').innerHTML = 
                    '<h3>Response Submitted Successfully!</h3>' +
                    '<p style="margin-top: 10px;">Your response has been saved.</p>' +
                    '<p style="margin-top: 5px; font-size: 13px;">The tracker will automatically import it. The QC Reviewer will be notified once all reviewers submit.</p>' +
                    '<button class="open-folder-btn" onclick="closeWindow()" style="margin-top: 15px;">Close Window</button>';
                document.getElementById('success-message').className = 'success-box';
            } catch (e) {
                alert("Error saving response: " + e.message + "\\nPath: " + filePath);
            }
        }
        
        function closeWindow() {
            window.close();
        }
    </script>
</body>
</html>
